import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  flex: 1;
`;

export function Inspector(): JSX.Element {
  return <Container>Inspector!</Container>;
}
